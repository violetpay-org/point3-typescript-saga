{
    "name": "point3-typescript-saga",
    "version": "0.0.1",
    "devDependencies": {
        "tsup": "^8.1.0"
    },
    "scripts": {
        "build": "tsup",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
